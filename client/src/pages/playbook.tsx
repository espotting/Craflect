import { DashboardLayout } from "@/components/layout";
export default function PlaybookPage() {
  return (
    <DashboardLayout>
      <div style={{ background: '#08080f', minHeight: '100vh', padding: 32, color: 'rgba(255,255,255,0.4)', fontSize: 14 }}>
        Playbook — Sprint 4
      </div>
    </DashboardLayout>
  );
}
