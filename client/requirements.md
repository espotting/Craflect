## Packages
framer-motion | Page transitions and smooth entry animations for a premium feel
lucide-react | High-quality icons for the sidebar and UI elements
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge Tailwind classes

## Notes
- The application enforces a dark mode exclusively for the requested sleek, neon SaaS aesthetic.
- Relies on Replit Auth (`/api/login`, `/api/logout`, `/api/auth/user`).
- Background video for landing page is expected at `/hero-background.mp4`.
